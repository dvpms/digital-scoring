# mpm_prefork tuning configuration.
# mpm_event and mpm_worker .so files are removed at build time and their
# modules are never enabled, so only mpm_prefork is active.

# Suppress the "Could not reliably determine the server's fully qualified
# domain name" warning that Apache logs on startup when no ServerName is set.
ServerName localhost
<IfModule mpm_prefork_module>
    StartServers          5
    MinSpareServers       5
    MaxSpareServers      10
    MaxRequestWorkers   150
    MaxConnectionsPerChild 0
</IfModule>
